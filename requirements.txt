# FedSanitize — Pinned Dependencies
# Pin ALL versions. An unpinned SciPy/Streamlit/PyTorch bump mid-build
# can silently change API behaviour (e.g. Wasserstein signature).
# Confirm current stable versions before locking, but always pin something.

torch==2.3.1
torchvision==0.18.1
numpy==1.26.4
pandas==2.2.2
scipy==1.13.1
scikit-learn==1.5.0
streamlit==1.36.0
plotly==5.22.0
matplotlib==3.9.0
